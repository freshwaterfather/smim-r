% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smim_fit.R
\name{smim_fit}
\alias{smim_fit}
\title{Fit the SMIM to a prepared breakthrough curve with multi-start bounded least squares}
\usage{
smim_fit(
  tobs,
  cobs,
  L,
  objective = c("volponi", "log"),
  bounds = smim_bounds(),
  starts = 60L,
  seed = 1L,
  inject_duration = 0,
  tol = 1e-14,
  max_nfev = 10000L,
  max_iter = 10000L,
  verbose = TRUE
)
}
\arguments{
\item{tobs, cobs}{fitted points from \code{\link[=prepare_btc]{prepare_btc()}}. @param L reach length (m).}

\item{objective}{\code{"volponi"} (original) or \code{"log"}. See \code{\link[=objective_volponi]{objective_volponi()}}.}

\item{bounds}{list from \code{\link[=smim_bounds]{smim_bounds()}}.}

\item{starts}{either a numeric matrix of start points (one per row, 6 columns; e.g.
the matrix exported by the MATLAB driver, for validation), or an integer number of
Latin-hypercube starts to draw with \code{\link[=lhs_bounded]{lhs_bounded()}} (the bounds mid-point guess is
appended as the last row, as \code{runFit.m} does).}

\item{seed}{seed for \code{\link[=lhs_bounded]{lhs_bounded()}} when \code{starts} is a number.}

\item{inject_duration}{injection duration (s).}

\item{tol}{tolerance used for \code{ftol}, \code{xtol}, \code{gtol} (\code{runFit.m}: \code{1e-14}).}

\item{max_nfev, max_iter}{limits per start (\code{runFit.m}: \code{10000}).}

\item{verbose}{print progress.}
}
\value{
list: \code{params} (best refined), \code{resnorm}, \code{residual}, \code{jacobian}, \code{ccfit_raw},
\code{ccfit_norm}, \code{se_volponi}, \code{se_consistent}, \code{diagnostics}, \code{per_start}
(data frame: every local solution), \code{best_start}, \code{starts}, \code{bounds}, \code{objective}.
}
\description{
R counterpart of \code{runFit.m}: a bounded trust-region least-squares run (\code{\link[=trf_lsq]{trf_lsq()}})
from every start point, selection of the best local solution (lowest sum of squares
among runs that converged, else lowest overall — \code{MultiStart}'s rule), and one final
refinement from that point which also yields the Jacobian for standard errors.
}
