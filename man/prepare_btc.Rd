% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_btc}
\alias{prepare_btc}
\title{Prepare a breakthrough curve for fitting, following Volponi's \code{Lead_SMIM.m} + \code{SMIMfit.m}}
\usage{
prepare_btc(
  t,
  C,
  hit_time_s = -Inf,
  t_end_s = max(t),
  Q_L_s = NA,
  mass_ug = NA
)
}
\arguments{
\item{t}{elapsed time since release (s). @param C corrected concentration (µg L⁻¹).}

\item{hit_time_s}{arrival time (s); samples before it are zeroed (then dropped).}

\item{t_end_s}{last time retained (s). Default: the last sample.}

\item{Q_L_s}{discharge (L s⁻¹) and \code{mass_ug} injected mass (µg), for the recovery
fraction only (they do not affect the normalised curve).}
}
\value{
list with \code{tobs}, \code{cobs} (fitted points), \code{t_all}, \code{C_all}, \code{cnorm_all} (the
cleaned window before steps 6–7), \code{mass_recovery_fraction}, and counts
\code{n_neg_clamped}, \code{n_prehit_zeroed}, \code{n_dropped_le0}.
}
\description{
Reproduces, in order, the data handling of the original MATLAB workflow so that the R
fit sees exactly the same numbers as the MATLAB fit:
\enumerate{
\item keep samples with \code{t >= 0} (release onward; \code{Lead_SMIM.m} lines 68–73);
\item set negative concentrations to zero (\code{Lead_SMIM.m} line 57) — this only affects the
normalisation integral, because those samples are deleted in step 7;
\item set samples before \code{hit_time} to zero (\code{Lead_SMIM.m} lines 76–77);
\item drop samples after \code{t_end} (\code{Lead_SMIM.m} lines 85–88);
\item normalise to unit area with the trapezoidal rule (\code{cNorm.m}, which reduces to
\code{c / trapz(t, c)} whatever \code{Q} is) and compute the mass-recovery fraction
\code{Q * trapz(t, c) / mass};
\item drop the sample at \code{t == 0} (\code{SMIMfit.m} lines 127–130);
\item drop every sample with \code{C <= 0} (\code{SMIMfit.m} lines 133–135).
}
}
\details{
No other transformation is applied: no smoothing, binning, clamping of the fitted
values or imputation. The concentration column must already be gain- and
background-corrected.
}
