% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smim_forward.R
\name{lap_psi_tpl}
\alias{lap_psi_tpl}
\title{Laplace-domain truncated-power-law waiting-time transform}
\usage{
lap_psi_tpl(s, param)
}
\arguments{
\item{s}{complex vector of Laplace variables.}

\item{param}{numeric \code{c(beta, log10(T1), log10(T2))} — the exponentiation is done here,
as in the original.}
}
\value{
complex vector.
}
\description{
Port of \code{LapPsiFuncTPL.m} (case \code{'TPL'}). With \code{tau = T1/T2}, \verb{x = tau + T1 s},
\deqn{\tilde\varphi(s) = [\tau^\beta e^\tau \Gamma(-\beta,\tau)]^{-1}\, x^\beta e^x \Gamma(-\beta, x).}
MATLAB compares the complex \code{x > 1} by real part only; the same rule is used here. The
\code{Re(x) > 1} Gauss–Laguerre branch of the original is reproduced for completeness, but
note that the MATLAB original errors in that branch (scalar \code{denom} indexed by a
vector mask), so it can never have contributed to a published fit; a warning is issued.
}
\keyword{internal}
