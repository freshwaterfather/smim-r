% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_smim_params}
\alias{plot_smim_params}
\title{Fitted SMIM parameters per stream as a four-panel bar figure}
\usage{
plot_smim_params(results, fill = "grey55")
}
\arguments{
\item{results}{data frame with columns \code{site}, \code{v_m_s}, \code{D_m2_s}, \code{Lambda_1_s}, \code{beta}
and optionally \code{se_v}, \code{se_D}, \code{se_Lambda}, \code{se_beta} (as written by
\code{analysis/example_four_streams.R}).}

\item{fill}{bar fill colour (grayscale-safe default).}
}
\value{
a \code{ggplot} object (2 × 2 facets with the parameter label on the y axis).
}
\description{
One bar per stream for each of the four fitted parameters, in the layout and with the
axis labels of Volponi et al. (2025, Fig. 5): velocity (top left), dispersion (top
right), exchange rate (bottom left) and power-law slope (bottom right). Thin error bars
show ± one standard error when the \verb{se_*} columns are present.
}
