% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invlap_dehoog.R
\name{invlap_dehoog}
\alias{invlap_dehoog}
\title{Numerical inverse Laplace transform (de Hoog, Knight & Stokes 1982)}
\usage{
invlap_dehoog(F, t, tol = 1e-10, M = 25L)
}
\arguments{
\item{F}{function of a complex vector \code{s} returning the Laplace-domain values.}

\item{t}{numeric vector of times, all \verb{> 0}.}

\item{tol}{accuracy parameter (\code{getTolerance.m}: \code{1e-10}).}

\item{M}{number of terms (\code{getTolerance.m}: \code{25}).}
}
\value{
numeric vector \code{f(t)} in the order of \code{t} \strong{sorted by decade group} — exactly
as MATLAB's \code{cell2mat} returns it. For a monotone increasing \code{t} this is the input
order; for other orders the result is permuted the same way MATLAB permutes it.
}
\description{
Line-for-line port of Volponi/Schmidt's \code{invLap_deHoog.m}: the time vector is split
into decades (\code{floor(log10(t))}) and each group is inverted separately with
\code{T = 2 * max(t)}, \verb{gamma = -log(tol) / (2 T)}, \verb{2M + 1} terms of the continued
fraction, and the same quotient–difference recurrences and remainder term.
}
\references{
de Hoog, F. R., Knight, J. H., & Stokes, A. N. (1982). An improved method
for numerical inversion of Laplace transforms. SIAM J. Sci. Stat. Comput. 3, 357–366.
\doi{10.1137/0903022}
}
