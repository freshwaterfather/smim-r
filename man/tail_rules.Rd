% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tail_cutoff.R
\name{tail_rules}
\alias{tail_rules}
\alias{noise_sigma}
\alias{find_hit_time}
\alias{find_tail_cutoff}
\title{Noise level, arrival time and detection-limit tail cutoff for a breakthrough curve}
\usage{
noise_sigma(x, resolution = 0)

find_hit_time(t, C, sigma, frac = 0.005)

find_tail_cutoff(t, C, thr, min_run = 5L, k = 5L)
}
\arguments{
\item{x}{background (pre-release) concentrations for \code{noise_sigma()}.}

\item{resolution}{sensor quantisation step in concentration units (e.g. ADC step × gain).}

\item{t, C}{time and background-corrected concentration vectors, in time order.}

\item{sigma}{noise SD from \code{noise_sigma()}. @param frac peak fraction floor (0.005).}

\item{thr}{threshold (e.g. \code{3 * sigma} or \code{10 * sigma}).}

\item{min_run}{minimum number of trailing samples below \code{thr}. @param k running-median window.}
}
\value{
\code{noise_sigma()}: list \code{sigma}, \code{sigma_mad}, \code{sigma_sd}, \code{source};
\code{find_hit_time()}: list \code{hit_time_s}, \code{threshold}; \code{find_tail_cutoff()}: a time or \code{NA}.
}
\description{
Rule-based helpers applied identically to every curve. They only \emph{locate} times; the
raw data are what get fitted (no clamping, imputation, binning or smoothing of the
values). The conventions follow the MATLAB workflow of Volponi (2024): samples before
the arrival time are set to zero and later deleted, and the fit window ends at \code{t_end}.
}
\details{
\itemize{
\item \code{noise_sigma()} — robust noise SD (1.4826 × MAD) of the background samples, floored
at the quantisation SD of the sensor (\code{resolution / sqrt(12)}) for flat traces.
\item \code{find_hit_time()} — arrival time: the start of the final contiguous run of samples
above \verb{max(3 sigma, frac * peak)} that ends at the peak. Walking back from the peak is
robust to isolated pre-arrival blips; the peak-relative floor rejects small offset
shifts (0.1–0.8 µg/L) seen right after release, which sit above 3σ but far below the
true leading edge.
\item \code{find_tail_cutoff()} — the last time before a running-median-smoothed curve stays
below a threshold (LOD = 3σ or LOQ = 10σ) for the rest of the record (at least
\code{min_run} samples). \code{NA} when the curve never stays below the threshold, i.e. when
the record ends before the tail reaches the detection limit.
}
}
\examples{
t <- seq(-300, 2400, by = 30)
C <- 300 * dgamma(pmax(t, 0) / 100, 6, 1) + rnorm(length(t), 0, 0.02)
s <- noise_sigma(C[t < 0], resolution = 0.015)
find_hit_time(t, C, s$sigma)
find_tail_cutoff(t, C, 3 * s$sigma)
}
