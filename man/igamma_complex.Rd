% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igamma_complex.R
\name{igamma_complex}
\alias{igamma_complex}
\title{Upper incomplete gamma function for negative order and complex argument}
\usage{
igamma_complex(a, z)
}
\arguments{
\item{a}{real scalar order, \eqn{-3 < a \le 0} (checked).}

\item{z}{complex (or real) vector, \eqn{|z| \le 20}. The series is only used in this
range; the SMIM model needs \eqn{|z| \ll 1}.}
}
\value{
complex vector, same length as \code{z}.
}
\description{
Computes the non-regularised upper incomplete gamma function
\eqn{\Gamma(a, z) = \int_z^\infty t^{a-1} e^{-t} dt} for real order
\eqn{a \le 0} (typically \eqn{a = -\beta} with \eqn{\beta \in [0, 2]}) and complex
argument \eqn{z}, matching MATLAB's Symbolic Math Toolbox \code{igamma(a, z)}, which
Volponi's \code{LapPsiFuncTPL.m} relies on. Base R has no complex incomplete gamma.
}
\details{
The ascending series
\deqn{\Gamma(a,z) = \Gamma(a) - \sum_{n \ge 0} \frac{(-1)^n z^{a+n}}{n!\,(a+n)}}
is used. At and near non-positive integers \eqn{a = -m} both \eqn{\Gamma(a)} and the
\eqn{n = m} term diverge and cancel; that pair is evaluated in closed, cancellation-free
form with \code{expm1}, so the function is accurate for every \eqn{\beta} in the fit bounds,
including exactly 0, 1 and 2 (the bounds and the mid-point guess). Principal branches
are used for \eqn{z^a} and \eqn{\log z}, as in MATLAB.
}
\references{
de Hoog, F. R., Knight, J. H., & Stokes, A. N. (1982). SIAM J. Sci. Stat.
Comput. 3, 357–366 (context); Temme, N. M. (1996) \emph{Special Functions}, §11.2
(near-integer handling).
}
