% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lhs_bounded.R
\name{lhs_bounded}
\alias{lhs_bounded}
\title{Latin-hypercube start points within bounds (port of \code{lhsdesignbnd.m})}
\usage{
lhs_bounded(n, lb, ub, seed = 1L)
}
\arguments{
\item{n}{number of points. @param lb,ub bounds (length p).}

\item{seed}{integer seed (\code{set.seed}), recorded in the attribute \code{"seed"}.}
}
\value{
\verb{n x p} matrix with attribute \code{seed}.
}
\description{
\code{lhsdesignbnd.m} (Rik Blok, 2014, BSD-2) draws a unit Latin hypercube with MATLAB's
\code{lhsdesign} (default \verb{'criterion','maximin','iterations',5}) and rescales each column
to \verb{[lb, ub]}. Here the unit design comes from \code{lhs::maximinLHS()} when available,
otherwise \code{lhs::randomLHS()}, otherwise a plain stratified draw. Columns with
\code{lb == ub} are constant. R's and MATLAB's random streams differ, so this function is
for \strong{independent} runs; for validation against MATLAB use the exported start-point
matrix instead (see \code{\link[=smim_fit]{smim_fit()}} argument \code{starts}).
}
