% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trf.R
\name{trf_lsq}
\alias{trf_lsq}
\title{Bounded nonlinear least squares by a trust-region reflective method}
\usage{
trf_lsq(
  fun,
  x0,
  lb = -Inf,
  ub = Inf,
  ftol = 1e-08,
  xtol = 1e-08,
  gtol = 1e-08,
  max_nfev = Inf,
  max_iter = 1000L,
  fd_step = .Machine$double.eps^(1/3),
  verbose = FALSE
)
}
\arguments{
\item{fun}{function returning the residual vector for a parameter vector.}

\item{x0}{starting point (moved strictly inside the bounds if necessary).}

\item{lb, ub}{bounds (finite or infinite). Parameters with \code{lb == ub} are held fixed.}

\item{ftol, xtol, gtol}{tolerances on the relative cost change, the step, and the scaled
gradient (MATLAB's \code{FunctionTolerance}, \code{StepTolerance}, \code{OptimalityTolerance}).}

\item{max_nfev}{maximum number of residual evaluations (counting Jacobian columns),
like MATLAB's \code{MaxFunctionEvaluations}; \code{max_iter} like \code{MaxIterations}.}

\item{fd_step}{finite-difference base step (\code{eps^(1/3)} = MATLAB's central default).}

\item{verbose}{print one line per iteration.}
}
\value{
list: \code{x}, \code{resnorm} (sum of squares), \code{residual}, \code{jacobian} (central FD at
\code{x}, same rule as MATLAB), \code{exitflag} (1 gtol, 2 ftol, 3 xtol, 0 limit reached),
\code{iterations}, \code{nfev}, \code{optimality}, \code{message}.
}
\description{
An R implementation of the trust-region reflective (TRF) algorithm of Branch, Coleman
& Li (1999) for bound-constrained least squares, following the structure of SciPy's
\code{least_squares(method = "trf")} (the "exact" SVD-based trust-region subproblem, the
reflective step selection and the Coleman–Li scaling). It is the R counterpart of
MATLAB's \code{lsqnonlin} default \code{'trust-region-reflective'} algorithm used in Volponi's
\code{runFit.m}. The two implementations share the Coleman–Li affine scaling and the
reflective step but differ in the subproblem solver (MATLAB: 2-D subspace with
preconditioned conjugate gradients) and in the exact form of the stopping rules, so
iterates are not bit-identical; equivalence is judged on the converged solutions.
}
\details{
Jacobians are formed by central finite differences with MATLAB's default step,
\verb{eps^(1/3) * max(|x|, 1)}, kept inside the bounds by falling back to a one-sided
difference when a central step would cross a bound.
}
\references{
Branch, M. A., Coleman, T. F., & Li, Y. (1999). A subspace, interior, and
conjugate gradient method for large-scale bound-constrained minimization problems.
SIAM J. Sci. Comput. 21, 1–23. Coleman, T. F., & Li, Y. (1996). An interior trust
region approach for nonlinear minimization subject to bounds. SIAM J. Optim. 6, 418–445.
}
