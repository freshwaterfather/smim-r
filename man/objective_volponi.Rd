% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objectives.R
\name{objective_volponi}
\alias{objective_volponi}
\alias{objective_log}
\title{Residual vectors for the SMIM fit}
\usage{
objective_volponi(
  params,
  tobs,
  cobs,
  L,
  inject_duration = 0,
  forward = smim_forward
)

objective_log(
  params,
  tobs,
  cobs,
  L,
  inject_duration = 0,
  forward = smim_forward
)
}
\arguments{
\item{params}{numeric length 6, see \code{\link[=smim_forward]{smim_forward()}}.}

\item{tobs, cobs}{observation times and (area-normalised, strictly positive) concentrations.}

\item{L}{reach length (m). @param inject_duration injection duration (s).}

\item{forward}{the forward model, by default \code{\link[=smim_forward]{smim_forward()}} (injectable for tests).}
}
\value{
numeric residual vector of length \code{length(cobs)}.
}
\description{
\code{objective_volponi()} is a line-for-line port of \code{objFunctionSMIM.m} (Volponi/Schmidt,
after Kelly et al. 2017 FracFit): the model curve is normalised to unit area over the
observation times with the trapezoidal rule, and the residuals are
\eqn{r_i = |C_{obs,i} - C_{fit,i}| / \sqrt{N C_{obs,i}}}, so that a least-squares
solver minimises \eqn{\sum (C_{obs} - C_{fit})^2 / (N C_{obs})}. Any \code{NaN} in the model
curve returns a vector of \code{1e16}. \code{K_mass} is always 1 in the original and is not
exposed.
}
\details{
\code{objective_log()} is the log-residual alternative added for this port (validated
against \code{validation/matlab/objFunctionSMIM_log.m}): \eqn{r_i = \log C_{obs,i} - \log C_{fit,i}}
on the same normalised curve, with the same \code{1e16} penalty when the curve has a \code{NaN}
or a non-positive value.

\strong{Which objective?} The two weight the curve differently. The original 1/C weighting
lets the high-concentration peak dominate, so velocity and the peak shape are matched
best and the model may fall below a long, low tail. Log residuals treat every point in
relative terms, so the many low-concentration tail points dominate and the tail-derived
parameters (\eqn{\Lambda}, \eqn{eta}) follow the observed tail, at some cost near the
peak. Use \code{"volponi"} to reproduce the published method; use \code{"log"} when the goal is to
characterise retention from the tail. On the example data the two give log-scale
\eqn{R^2} of about 0.98 and 0.998 and differ in \eqn{eta} by roughly 30–40 \%.
}
\references{
Kelly, J. F., Bolster, D., Meerschaert, M. M., Drummond, J. D., & Packman,
A. I. (2017). FracFit: A robust parameter estimation tool for fractional calculus
models. Water Resources Research, 53, 2559–2567. \doi{10.1002/2016WR019748}
}
